#pragma once

#include <glm/glm.hpp>

// Converts glm types to Ocean types for easier use / readability.

// This will be a slow conversion project, as it is intensely repetative.

namespace Ocean {

	/* --- Vector (Math) Types --- */
	// Bool

	// Vec2 bool
	// A 2 component vector with boolean types.
	//

	// Int

	// Float

	// Etc.

}	// Ocean