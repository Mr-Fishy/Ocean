#include "Ocean/Core/Types/Vector2D.hpp"
#include "Ocean/Core/Types/Vector3D.hpp"
#include "Ocean/Core/Types/Vector4D.hpp"

#include "Ocean/Core/Types/Matrix4D.hpp"

// Vec2

template struct vec<2, u8,  LOW_PRECISION>;
template struct vec<2, u16, LOW_PRECISION>;
template struct vec<2, u32, LOW_PRECISION>;
template struct vec<2, u64, LOW_PRECISION>;

template struct vec<2, i8,  LOW_PRECISION>;
template struct vec<2, i16, LOW_PRECISION>;
template struct vec<2, i32, LOW_PRECISION>;
template struct vec<2, i64, LOW_PRECISION>;

template struct vec<2, f32, LOW_PRECISION>;
template struct vec<2, f64, LOW_PRECISION>;



template struct vec<2, u8,  MEDIUM_PRECISION>;
template struct vec<2, u16, MEDIUM_PRECISION>;
template struct vec<2, u32, MEDIUM_PRECISION>;
template struct vec<2, u64, MEDIUM_PRECISION>;

template struct vec<2, i8,  MEDIUM_PRECISION>;
template struct vec<2, i16, MEDIUM_PRECISION>;
template struct vec<2, i32, MEDIUM_PRECISION>;
template struct vec<2, i64, MEDIUM_PRECISION>;

template struct vec<2, f32, MEDIUM_PRECISION>;
template struct vec<2, f64, MEDIUM_PRECISION>;



template struct vec<2, u8,  HIGH_PRECISION>;
template struct vec<2, u16, HIGH_PRECISION>;
template struct vec<2, u32, HIGH_PRECISION>;
template struct vec<2, u64, HIGH_PRECISION>;

template struct vec<2, i8,  HIGH_PRECISION>;
template struct vec<2, i16, HIGH_PRECISION>;
template struct vec<2, i32, HIGH_PRECISION>;
template struct vec<2, i64, HIGH_PRECISION>;

template struct vec<2, f32, HIGH_PRECISION>;
template struct vec<2, f64, HIGH_PRECISION>;

// Vec3

template struct vec<3, u8,  LOW_PRECISION>;
template struct vec<3, u16, LOW_PRECISION>;
template struct vec<3, u32, LOW_PRECISION>;
template struct vec<3, u64, LOW_PRECISION>;

template struct vec<3, i8,  LOW_PRECISION>;
template struct vec<3, i16, LOW_PRECISION>;
template struct vec<3, i32, LOW_PRECISION>;
template struct vec<3, i64, LOW_PRECISION>;

template struct vec<3, f32, LOW_PRECISION>;
template struct vec<3, f64, LOW_PRECISION>;



template struct vec<3, u8,  MEDIUM_PRECISION>;
template struct vec<3, u16, MEDIUM_PRECISION>;
template struct vec<3, u32, MEDIUM_PRECISION>;
template struct vec<3, u64, MEDIUM_PRECISION>;

template struct vec<3, i8,  MEDIUM_PRECISION>;
template struct vec<3, i16, MEDIUM_PRECISION>;
template struct vec<3, i32, MEDIUM_PRECISION>;
template struct vec<3, i64, MEDIUM_PRECISION>;

template struct vec<3, f32, MEDIUM_PRECISION>;
template struct vec<3, f64, MEDIUM_PRECISION>;



template struct vec<3, u8,  HIGH_PRECISION>;
template struct vec<3, u16, HIGH_PRECISION>;
template struct vec<3, u32, HIGH_PRECISION>;
template struct vec<3, u64, HIGH_PRECISION>;

template struct vec<3, i8,  HIGH_PRECISION>;
template struct vec<3, i16, HIGH_PRECISION>;
template struct vec<3, i32, HIGH_PRECISION>;
template struct vec<3, i64, HIGH_PRECISION>;

template struct vec<3, f32, HIGH_PRECISION>;
template struct vec<3, f64, HIGH_PRECISION>;
