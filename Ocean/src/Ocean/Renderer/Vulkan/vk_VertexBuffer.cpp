#include "vk_VertexBuffer.hpp"

namespace Ocean {

    namespace Splash {

        vkVertexBuffer::vkVertexBuffer(u32 size) : m_VertexBuffer(), m_BufferMemory(), m_Layout() {
            
        }

        vkVertexBuffer::vkVertexBuffer(f32* vertices, u32 size) : m_VertexBuffer(), m_BufferMemory(), m_Layout() {
            
        }

        vkVertexBuffer::~vkVertexBuffer() {
            
        }

        void vkVertexBuffer::Bind() const {
            
        }

        void vkVertexBuffer::Unbind() const {
            
        }

        void vkVertexBuffer::SetData(const void* data, u32 size) {
            
        }

    }   // Splash

}   // Ocean
